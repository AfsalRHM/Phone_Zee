<%- include('../adminLayouts/header.ejs') %>

<%- include('../adminLayouts/pageHeader.ejs') %>


<section class="content-main" style="background-color: rgb(236, 236, 236);">
    <div class="content-header" style="background-color: rgb(236, 236, 236);">
        <% if (comingFor == 'editProduct' ) { %>
                <h2 class="content-title">Edit Offer</h2>
            </div>
            <section class="content-main d-flex justify-content-center align-items-center">
                <div class="card">
                    <div class="card-body">
                        <h2 class="content-title card-title mb-4">Edit Product Offer</h2>
                        <form method="post">
                            <% if (typeof editOfferMessage !== 'undefined') { %>
                                <p style="color: red;" class="d-inline-flex p-2 " ><%= editOfferMessage %>*</p>
                            <% } %>
                            <input type="hidden" name="item" value="product">
                            <div class="mb-4">
                                <label for="productName" class="form-label">Product Name</label>
                                <select class="form-select" name="itemName">
                                    <% for (let i = 0; i < items.length; i++) { %>
                                        <% if (items[i].name == offerData.item_Id.name ) { %>
                                            <option value="<%= items[i].name %>" selected ><%= items[i].name %></option>
                                        <% } else { %>
                                            <option value="<%= items[i].name %>"><%= items[i].name %></option>
                                        <% } %>
                                    <% }; %>
                                </select>
                            </div>
        <% } else if (comingFor == 'editCategory' ) { %>
            <h2 class="content-title">Add Offer</h2>
            </div>
            <section class="content-main d-flex justify-content-center align-items-center">
                <div class="card">
                    <div class="card-body">
                        <h2 class="content-title card-title mb-4">Edit Categroy Offer</h2>
                        <form method="post">
                            <% if (typeof editOfferMessage !== 'undefined') { %>
                                <p style="color: red;" class="d-inline-flex p-2 " ><%= editOfferMessage %>*</p>
                            <% } %>
                            <input type="hidden" name="item" value="category">
                            <div class="mb-4">
                                <label for="categoryName" class="form-label">Category Name</label>
                                <p><%= offerData.item_Id.name %></p>
                                <select class="form-select" name="itemName">
                                    <% for (let i = 0; i < items.length; i++) { %>
                                        <% if (items[i].name == offerData.item_Id.name ) { %>
                                            <option value="<%= items[i].name %>" selected ><%= items[i].name %></option>
                                        <% } else { %>
                                            <option value="<%= items[i].name %>"><%= items[i].name %></option>
                                        <% } %>
                                    <% }; %>
                                </select>
                            </div>
        <% } %>
                    <div class="mb-4">
                        <label for="percentageOffer" class="form-label">Percentage Offer(%)</label>
                        <input type="number" class="form-control" value="<%= offerData.offer_percentage %>" name="percentageOffer" max="99" min="0" placeholder="Enter percentage offer">
                    </div>
                    <div class="d-grid justify-content-center">
                        <div>
                            <button type="submit" class="btn btn-primary">Edit Offer</button>
                        </div>
                    </div>
                </form>
            </div>
            <!-- card body .// -->
        </div>
        <!-- card .// -->
    </section>
    <!-- card end// -->
</section>
<!-- content-main end// -->

<%- include('../adminLayouts/pageFooter.ejs') %>

<%- include('../adminLayouts/footer.ejs') %>
